bin/
*.exe
*.test
*.out
.DS_Store

# nix build / nixos-rebuild output symlinks
result
result-*

# Local git worktrees used by Claude Code sessions; the outer repo
# sees them as embedded submodules if committed by accident.
.claude/worktrees/

# Claude Code session state — runtime locks / queues the harness
# manages outside the repo's content.
.claude/scheduled_tasks.lock
