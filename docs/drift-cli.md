# drift CLI reference

This file is generated by `.claude/skills/drift-docs/scripts/dump_cli.sh`. Do not edit by hand — run the drift-docs skill to regenerate.

---

## `drift --help`

```text
Usage: drift <command> [flags]

drift — stateless client for remote devcontainer workspaces.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

Commands:
  help [flags]
    Print an LLM-friendly command + protocol reference.

  circuit add <user@host> [flags]
    Register a circuit (probes for name, updates client config + SSH config).

  circuit rm <name>
    Unregister a circuit.

  circuit list
    List configured circuits.

  circuit set name <new-name>
    Rename the target circuit (rewrites server config + local alias).

  circuit set default [<name>]
    Choose which configured circuit is the default (interactive picker when no
    name given).

  init [flags]
    Interactive first-time setup wizard (circuits + characters).

  status [flags]
    Show configured circuits + their lakitu health and kart counts.

  new <name> [flags]
    Create a new kart (from starter or existing repo).

  list [flags]
    List karts on the target circuit.

  info <name> [flags]
    Show a single kart's info.

  start <name> [flags]
    Start a kart (idempotent).

  stop <name> [flags]
    Stop a kart (idempotent).

  restart <name> [flags]
    Restart a kart.

  delete <name> [flags]
    Delete a kart (errors if missing).

  logs <name> [flags]
    Fetch a chunk of kart logs.

  enable <name> [flags]
    Enable kart autostart on circuit reboot (idempotent).

  disable <name> [flags]
    Disable kart autostart (idempotent).

  connect (into,attach) <name> [flags]
    Connect to a kart via mosh (ssh fallback); auto-starts if stopped.

  runs [flags]
    List server-side shorthand commands (see drift run).

  run <name> [<args> ...] [flags]
    Execute a server-side shorthand (e.g. drift run ai).

  migrate [flags]
    Adopt an existing devpod workspace as a drift kart (interactive).

  update [flags]
    Check GitHub for a newer drift release and self-install.

Run "drift <command> --help" for more information on a command.
drift: expected one of "help", "circuit", "init", "status", "new", ...
exit status 2
```

## `drift help --full`

```text
NAME
  drift — stateless client for remote devcontainer workspaces.

drift is the stateless workstation client. Every non-local call shells out
to ssh and invokes lakitu on a circuit via JSON-RPC 2.0. Server state lives
on the circuit under ~/.drift/garage/.

COMMANDS (run `drift <cmd> --help` for flags)
  circuit add — Register a circuit (probes for name, updates client config + SSH config).
  circuit list — List configured circuits.
  circuit rm — Unregister a circuit.
  circuit set default — Choose which configured circuit is the default (interactive picker when no name given).
  circuit set name — Rename the target circuit (rewrites server config + local alias).
  connect — Connect to a kart via mosh (ssh fallback); auto-starts if stopped.
  delete — Delete a kart (errors if missing).
  disable — Disable kart autostart (idempotent).
  enable — Enable kart autostart on circuit reboot (idempotent).
  help — Print an LLM-friendly command + protocol reference.
  info — Show a single kart's info.
  init — Interactive first-time setup wizard (circuits + characters).
  list — List karts on the target circuit.
  logs — Fetch a chunk of kart logs.
  migrate — Adopt an existing devpod workspace as a drift kart (interactive).
  new — Create a new kart (from starter or existing repo).
  restart — Restart a kart.
  run — Execute a server-side shorthand (e.g. drift run ai).
  runs — List server-side shorthand commands (see drift run).
  start — Start a kart (idempotent).
  status — Show configured circuits + their lakitu health and kart counts.
  stop — Stop a kart (idempotent).
  update — Check GitHub for a newer drift release and self-install.

RPC METHODS
  character.add
  character.list
  character.remove
  character.show
  chest.get
  chest.list
  chest.remove
  chest.set
  config.set
  config.show
  kart.delete
  kart.disable
  kart.enable
  kart.info
  kart.list
  kart.logs
  kart.migrate_list
  kart.new
  kart.restart
  kart.session_env
  kart.start
  kart.stop
  run.list
  run.resolve
  server.info
  server.init
  server.verify
  server.version
  tune.list
  tune.remove
  tune.set
  tune.show

EXIT CODES
  0 success · 2 user error · 3 not-found · 4 conflict

```

## Subcommands

### `drift circuit add --help`

```text
Usage: drift circuit add <user@host> [flags]

Register a circuit (probes for name, updates client config + SSH config).

Arguments:
  <user@host>    SSH destination, e.g. alice@devbox or alice@devbox:2222.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --default           Set as the default circuit.
      --no-ssh-config     Skip writing ~/.ssh/config and
                          ~/.config/drift/ssh_config.
drift: expected "<user@host>"
exit status 2
```

### `drift circuit list --help`

```text
Usage: drift circuit list

List configured circuits.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
NAME   HOST                DEFAULT
alpha  circuit@10.233.1.2  *
```

### `drift circuit rm --help`

```text
Usage: drift circuit rm <name>

Unregister a circuit.

Arguments:
  <name>    Circuit name to remove.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift circuit set default --help`

```text
Usage: drift circuit set default [<name>]

Choose which configured circuit is the default (interactive picker when no name
given).

Arguments:
  [<name>]    Circuit name to set as default. Omit for an interactive picker.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
circuits:
     NAME   HOST                
[1]  alpha  circuit@10.233.1.2  (current default)
pick (number, empty to cancel): aborted
exit status 1
```

### `drift circuit set name --help`

```text
Usage: drift circuit set name <new-name>

Rename the target circuit (rewrites server config + local alias).

Arguments:
  <new-name>    New circuit name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<new-name>"
exit status 2
```

### `drift connect --help`

```text
Usage: drift connect (into,attach) <name> [flags]

Connect to a kart via mosh (ssh fallback); auto-starts if stopped.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).
drift: expected "<name>"
exit status 2
```

### `drift delete --help`

```text
Usage: drift delete <name> [flags]

Delete a kart (errors if missing).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -y, --yes               Skip the interactive confirmation prompt.
drift: expected "<name>"
exit status 2
```

### `drift disable --help`

```text
Usage: drift disable <name> [flags]

Disable kart autostart (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift enable --help`

```text
Usage: drift enable <name> [flags]

Enable kart autostart on circuit reboot (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift help --help`

```text
Usage: drift help [flags]

Print an LLM-friendly command + protocol reference.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --full              Print the full Kong-derived catalog, including RPC
                          methods.
drift — Devpod for drifters
Remote devcontainers tuned for life on the move — persistent, agentic, phone-friendly

CIRCUITS
  circuit add|list|rm               Manage circuits (SSH config + client state)
  circuit set default|name          Set a circuit config field
  init                              Interactive first-time setup wizard
  status                            Show circuits + lakitu health + kart counts
  update                            Check GitHub for a newer drift and self-install

KARTS
  new <name>                        Create a kart (from starter or existing repo)
  list | info <name>                List karts on the circuit or show one's state
  start|stop|restart|delete <name>  Kart lifecycle (idempotent; delete is not)
  enable|disable <name>             Toggle kart autostart on circuit reboot
  logs <name>                       Fetch a chunk of kart logs
  connect|into|attach <name>        mosh (ssh fallback) into a kart; auto-starts
  migrate                           Adopt an existing devpod workspace as a drift kart (interactive)

RUNS
  runs                              List server-side shorthand commands
  run <name> [args...]              Execute a shorthand (e.g. run ai, run scaffolder)

Run `drift <cmd> --help` for flags · `drift help --full` for the full catalog
```

### `drift info --help`

```text
Usage: drift info <name> [flags]

Show a single kart's info.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift init --help`

```text
Usage: drift init [flags]

Interactive first-time setup wizard (circuits + characters).

Flags:
  -h, --help               Show context-sensitive help.
      --[no-]debug         Verbose output (default on; --no-debug to silence)
                           ($DRIFT_DEBUG).
      --no-color           Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING     Target circuit (overrides default).
  -o, --output="text"      Output format for structured commands.
  -v, --version            Print drift version and exit.

      --skip-circuits      Skip the circuit phase (assume already configured).
      --skip-characters    Skip the character phase.
      --no-probe           Skip the server.version probe (offline setup).

== Circuits ==
already configured:
  - alpha → circuit@10.233.1.2 (default)
Add a circuit? [y/N]: error: EOF
exit status 1
```

### `drift list --help`

```text
Usage: drift list [flags]

List karts on the target circuit.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
→ devpod list --output json
→ devpod status test2 --output json
NAME   STATUS   SOURCE                                             TUNE  AUTOSTART
test2  stopped  clone https://github.com/kurisu-agent/tzone-buddy  -     
```

### `drift logs --help`

```text
Usage: drift logs <name> [flags]

Fetch a chunk of kart logs.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -n, --tail=INT          Show the last N lines (0 = server default).
      --since=DURATION    Only records newer than duration (e.g. 10m, 1h).
                          JSONL only.
      --level=STRING      Minimum level (debug|info|warn|error). JSONL only.
      --grep=STRING       Substring match on msg (JSONL) or raw line (text).
drift: expected "<name>"
exit status 2
```

### `drift migrate --help`

```text
Usage: drift migrate [flags]

Adopt an existing devpod workspace as a drift kart (interactive).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift migrate requires an interactive terminal
exit status 1
```

### `drift new --help`

```text
Usage: drift new <name> [flags]

Create a new kart (from starter or existing repo).

Arguments:
  <name>    Kart name (matches ^[a-z][a-z0-9-]{0,62}$).

Flags:
  -h, --help                   Show context-sensitive help.
      --[no-]debug             Verbose output (default on; --no-debug to
                               silence) ($DRIFT_DEBUG).
      --no-color               Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING         Target circuit (overrides default).
  -o, --output="text"          Output format for structured commands.
  -v, --version                Print drift version and exit.

      --clone=STRING           Clone an existing repo (mutually exclusive with
                               --starter).
      --starter=STRING         Template repo; history is discarded after clone.
      --tune=STRING            Named preset that provides defaults for other
                               flags.
      --features=STRING        Devcontainer features JSON, merged with the
                               tune's (additive).
      --devcontainer=STRING    Override devcontainer: file path, JSON string,
                               or URL.
      --dotfiles=STRING        Layer-2 dotfiles repo URL (overrides tune's
                               dotfiles_repo).
      --character=STRING       Git/GitHub identity to inject.
      --autostart              Enable auto-start on server reboot.
drift: expected "<name>"
exit status 2
```

### `drift restart --help`

```text
Usage: drift restart <name> [flags]

Restart a kart.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift run --help`

```text
Usage: drift run <name> [<args> ...] [flags]

Execute a server-side shorthand (e.g. drift run ai).

Arguments:
  <name>          Shorthand name (see drift runs).
  [<args> ...]    Positional args forwarded to the remote command.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh) for interactive entries.
      --forward-agent     Enable SSH agent forwarding (-A).
drift: expected "<name>"
exit status 2
```

### `drift runs --help`

```text
Usage: drift runs [flags]

List server-side shorthand commands (see drift run).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: method "run.list" not implemented
  type: method_not_found
  method: run.list
exit status 2
```

### `drift start --help`

```text
Usage: drift start <name> [flags]

Start a kart (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift status --help`

```text
Usage: drift status [flags]

Show configured circuits + their lakitu health and kart counts.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --no-probe          Skip the server.version + kart.list round-trips (show
                          client state only).
→ devpod list --output json
→ devpod status test2 --output json
drift devel
default: alpha

   CIRCUIT  HOST                LAKITU  API  LATENCY  KARTS
→  alpha    circuit@10.233.1.2  dev     1    15ms     0/1
```

### `drift stop --help`

```text
Usage: drift stop <name> [flags]

Stop a kart (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift update --help`

```text
Usage: drift update [flags]

Check GitHub for a newer drift release and self-install.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --check             Check for an update without downloading.
→ checking latest release
current: devel
latest:  0.4.1
error: refusing to self-update a development build; rebuild from source or install a tagged release
exit status 1
```

