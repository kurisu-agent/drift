# drift CLI reference

This file is generated by `.claude/skills/drift-docs/scripts/dump_cli.sh`. Do not edit by hand — run the drift-docs skill to regenerate.

---

## `drift --help`

```text
Usage: drift <command> [flags]

drift — stateless client for remote devcontainer workspaces.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

Commands:
  help [flags]
    Print an LLM-friendly command + protocol reference.

  init [flags]
    Interactive first-time setup wizard (circuits + characters).

  status [flags]
    Show configured circuits + their lakitu health and per-circuit karts.

  update [<source>] [flags]
    Check GitHub for a newer drift release and self-install.

  circuits [flags]
    List configured circuits (table).

  karts [flags]
    List karts (cross-circuit by default; scope with -c).

  runs [flags]
    List runs.yaml entries on the target circuit.

  skills [flags]
    List Claude skills on the target circuit.

  circuit add <user@host> [flags]
    Register a circuit (probes for name, updates client config + SSH config).

  circuit rm <name>
    Unregister a circuit.

  circuit set name <new-name>
    Rename the target circuit (rewrites server config + local alias).

  circuit set icon [<icon>]
    Set the circuit's icon (nerd-font name like dev-go, or any single emoji).

  circuit set default [<name>]
    Choose which configured circuit is the default (interactive picker when no
    name given).

  circuit icons [<query>] [flags]
    Browse the shipped nerd-font icon catalog (filterable).

  circuit connect [<name> [<ssh-args> ...]] [flags]
    Open an interactive shell on the circuit's host (mosh/ssh).

  kart connect [<name> [<ssh-args> ...]] [flags]
    Connect to a kart via mosh (ssh fallback).

  kart info <name>
    Show a single kart's info.

  kart start [<name>]
    Start a kart (idempotent).

  kart stop [<name>]
    Stop a kart (idempotent).

  kart restart <name>
    Restart a kart.

  kart recreate <name>
    Recreate a kart's container (no tune re-apply) — picks up devcontainer.json
    changes.

  kart rebuild <name>
    Re-apply the current tune to a kart and recreate its container.

  kart delete [<name>] [flags]
    Delete a kart (errors if missing).

  kart logs <name> [flags]
    Fetch a chunk of kart logs.

  kart enable <name>
    Enable kart autostart on circuit reboot (idempotent).

  kart disable <name>
    Disable kart autostart (idempotent).

  connect (into,attach) [<name> [<ssh-args> ...]] [flags]
    Pick a circuit or kart and connect (merged picker).

  start [<name>] [flags]
    Start a kart (picker when no name).

  stop [<name>] [flags]
    Stop a kart (picker when no name).

  delete [<name>] [flags]
    Delete a kart (picker when no name).

  new <name> [flags]
    Create a new kart (from starter or existing repo).

  run [<name> [<args> ...]] [flags]
    Execute a user-script shorthand from runs.yaml.

  ai [flags]
    Launch Claude Code on the circuit (interactive REPL).

  skill [<name> [<prompt> ...]] [flags]
    Pick / invoke a Claude skill on the circuit.

  migrate [flags]
    Adopt an existing devpod workspace as a drift kart (interactive).

  ports list [flags]
    Show forwards (table or JSON; bare "drift ports" runs this).

  ports add <port> [flags]
    Add a forward (remaps on local-port conflict).

  ports rm (remove,delete) <port> [flags]
    Remove a forward.

  ports remap <spec> [flags]
    Change the local port for an existing forward (REMOTE:LOCAL).

  ports probe [flags]
    Probe in-kart listeners and pick which to forward.

  ports up [flags]
    Reconcile state to live forwards (no-op when already in sync).

  ports down [flags]
    Cancel forwards and stop the master (per-kart, or all when --all).

  ports status
    Print state + per-kart master liveness as JSON.

  browse [flags]
    Open filebrowser on the circuit (rooted at all kart workspaces) over an ssh
    tunnel.

Run "drift <command> --help" for more information on a command.
drift: expected one of "help", "init", "status", "update", "circuits", ...
exit status 2
```

## `drift help --full`

```text
NAME
  drift — stateless client for remote devcontainer workspaces.

drift is the stateless workstation client. Every non-local call shells out
to ssh and invokes lakitu on a circuit via JSON-RPC 2.0. Server state lives
on the circuit under ~/.drift/garage/.

COMMANDS (run `drift <cmd> --help` for flags)
  ai — Launch Claude Code on the circuit (interactive REPL).
  browse — Open filebrowser on the circuit (rooted at all kart workspaces) over an ssh tunnel.
  circuit add — Register a circuit (probes for name, updates client config + SSH config).
  circuit connect — Open an interactive shell on the circuit's host (mosh/ssh).
  circuit icons — Browse the shipped nerd-font icon catalog (filterable).
  circuit rm — Unregister a circuit.
  circuit set default — Choose which configured circuit is the default (interactive picker when no name given).
  circuit set icon — Set the circuit's icon (nerd-font name like dev-go, or any single emoji).
  circuit set name — Rename the target circuit (rewrites server config + local alias).
  circuits — List configured circuits (table).
  connect — Pick a circuit or kart and connect (merged picker).
  delete — Delete a kart (picker when no name).
  help — Print an LLM-friendly command + protocol reference.
  init — Interactive first-time setup wizard (circuits + characters).
  kart connect — Connect to a kart via mosh (ssh fallback).
  kart delete — Delete a kart (errors if missing).
  kart disable — Disable kart autostart (idempotent).
  kart enable — Enable kart autostart on circuit reboot (idempotent).
  kart info — Show a single kart's info.
  kart logs — Fetch a chunk of kart logs.
  kart rebuild — Re-apply the current tune to a kart and recreate its container.
  kart recreate — Recreate a kart's container (no tune re-apply) — picks up devcontainer.json changes.
  kart restart — Restart a kart.
  kart start — Start a kart (idempotent).
  kart stop — Stop a kart (idempotent).
  karts — List karts (cross-circuit by default; scope with -c).
  migrate — Adopt an existing devpod workspace as a drift kart (interactive).
  new — Create a new kart (from starter or existing repo).
  ports add — Add a forward (remaps on local-port conflict).
  ports down — Cancel forwards and stop the master (per-kart, or all when --all).
  ports list — Show forwards (table or JSON; bare "drift ports" runs this).
  ports probe — Probe in-kart listeners and pick which to forward.
  ports remap — Change the local port for an existing forward (REMOTE:LOCAL).
  ports rm — Remove a forward.
  ports status — Print state + per-kart master liveness as JSON.
  ports up — Reconcile state to live forwards (no-op when already in sync).
  run — Execute a user-script shorthand from runs.yaml.
  runs — List runs.yaml entries on the target circuit.
  skill — Pick / invoke a Claude skill on the circuit.
  skills — List Claude skills on the target circuit.
  start — Start a kart (picker when no name).
  status — Show configured circuits + their lakitu health and per-circuit karts.
  stop — Stop a kart (picker when no name).
  update — Check GitHub for a newer drift release and self-install.

RPC METHODS
  character.list
  character.new
  character.patch
  character.remove
  character.replace
  character.show
  chest.get
  chest.list
  chest.new
  chest.patch
  chest.remove
  circuit.browse_start
  circuit.browse_stop
  config.set
  config.show
  kart.connect
  kart.delete
  kart.disable
  kart.drift_check
  kart.enable
  kart.info
  kart.list
  kart.logs
  kart.migrate_list
  kart.new
  kart.probe_ports
  kart.rebuild
  kart.recreate
  kart.restart
  kart.session_env
  kart.start
  kart.stop
  run.list
  run.resolve
  server.info
  server.init
  server.status
  server.verify
  server.version
  skill.list
  skill.resolve
  tune.list
  tune.new
  tune.patch
  tune.remove
  tune.replace
  tune.show

EXIT CODES
  0 success · 2 user error · 3 not-found · 4 conflict

```

## Subcommands

### `drift ai --help`

```text
Usage: drift ai [flags]

Launch Claude Code on the circuit (interactive REPL).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).


tcgetattr: Inappropriate ioctl for device
exit status 1
```

### `drift browse --help`

```text
Usage: drift browse [flags]

Open filebrowser on the circuit (rooted at all kart workspaces) over an ssh
tunnel.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -l, --local-port=INT    Workstation port to bind (default: same as remote).
      --no-stop           Leave filebrowser running on the circuit after the
                          tunnel closes.
browsing /home/circuit/.drift/devpod/agent/contexts/default/workspaces
  http://localhost:31337
  Ctrl-C to disconnect.
bind [127.0.0.1]:31337: Address already in use
channel_setup_fwd_listener_tcpip: cannot listen to port: 31337
Could not request local forwarding.
error: ssh tunnel to drift.alpha collapsed after 103ms
  type: internal_error
  hints: [workstation port 31337 may already be in use (try --local-port) `ssh drift.alpha true` should succeed (alias missing or auth failed?) filebrowser may not be listening on the circuit at :31337 (rebuild lakitu on the circuit)]
  local_port: 31337
  remote_port: 31337
  ssh_exit: exit status 255
  ssh_stderr: bind [127.0.0.1]:31337: Address already in use
channel_setup_fwd_listener_tcpip: cannot listen to port: 31337
Could not request local forwarding.
exit status 1
```

### `drift circuit add --help`

```text
Usage: drift circuit add <user@host> [flags]

Register a circuit (probes for name, updates client config + SSH config).

Arguments:
  <user@host>    SSH destination, e.g. alice@devbox or alice@devbox:2222.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --default           Set as the default circuit.
      --no-ssh-config     Skip writing ~/.ssh/config and
                          ~/.config/drift/ssh_config.
drift: expected "<user@host>"
exit status 2
```

### `drift circuit connect --help`

```text
Usage: drift circuit connect [<name> [<ssh-args> ...]] [flags]

Open an interactive shell on the circuit's host (mosh/ssh).

Arguments:
  [<name>]            Circuit name; omit on a TTY to pick from a list.
  [<ssh-args> ...]    Extra flags forwarded to ssh (e.g. -- -i ~/.ssh/id_lab).
                      Under mosh, wrapped into --ssh="ssh …" for the bootstrap.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).
error: drift circuit connect requires a circuit name (non-interactive)
exit status 1
```

### `drift circuit icons --help`

```text
Usage: drift circuit icons [<query>] [flags]

Browse the shipped nerd-font icon catalog (filterable).

Arguments:
  [<query>]    Substring or fuzzy query (e.g. 'rocket', 'go', 'cat'). Empty
               lists the first --limit names.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --limit=60          Max rows to print. 0 means no cap.
GLYPH  NAME
      cod-account
      cod-activate_breakpoints
      cod-add
      cod-archive
      cod-arrow_both
      cod-arrow_circle_down
      cod-arrow_circle_left
      cod-arrow_circle_right
      cod-arrow_circle_up
      cod-arrow_down
      cod-arrow_left
      cod-arrow_right
      cod-arrow_small_down
      cod-arrow_small_left
      cod-arrow_small_right
      cod-arrow_small_up
      cod-arrow_swap
      cod-arrow_up
      cod-azure
      cod-azure_devops
      cod-beaker
      cod-beaker_stop
      cod-bell
      cod-bell_dot
      cod-bell_slash
      cod-bell_slash_dot
      cod-blank
      cod-bold
      cod-book
      cod-bookmark
      cod-bracket_dot
      cod-bracket_error
      cod-briefcase
      cod-broadcast
      cod-browser
      cod-bug
      cod-calendar
      cod-call_incoming
      cod-call_outgoing
      cod-case_sensitive
      cod-check
      cod-check_all
      cod-checklist
      cod-chevron_down
      cod-chevron_left
      cod-chevron_right
      cod-chevron_up
      cod-chip
      cod-chrome_close
      cod-chrome_maximize
      cod-chrome_minimize
      cod-chrome_restore
      cod-circle
      cod-circle_filled
      cod-circle_large
      cod-circle_large_filled
      cod-circle_slash
      cod-circle_small
      cod-circle_small_filled
      cod-circuit_board

(showing 60 — pass --limit=0 for the full list)
```

### `drift circuit rm --help`

```text
Usage: drift circuit rm <name>

Unregister a circuit.

Arguments:
  <name>    Circuit name to remove.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift circuits --help`

```text
Usage: drift circuits [flags]

List configured circuits (table).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
NAME   HOST                DEFAULT
alpha  circuit@10.233.1.2  *
```

### `drift circuit set default --help`

```text
Usage: drift circuit set default [<name>]

Choose which configured circuit is the default (interactive picker when no name
given).

Arguments:
  [<name>]    Circuit name to set as default. Omit for an interactive picker.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: circuit set default: name arg required on non-interactive stdin
  type: invalid_flag
exit status 2
```

### `drift circuit set icon --help`

```text
Usage: drift circuit set icon [<icon>]

Set the circuit's icon (nerd-font name like dev-go, or any single emoji).

Arguments:
  [<icon>]    Icon name (e.g. dev-go) or single emoji. Pass empty string to
              clear; omit for an interactive picker.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
circuit "alpha" has no icon set — try `drift circuit icons <query>` to browse
```

### `drift circuit set name --help`

```text
Usage: drift circuit set name <new-name>

Rename the target circuit (rewrites server config + local alias).

Arguments:
  <new-name>    New circuit name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<new-name>"
exit status 2
```

### `drift connect --help`

```text
Usage: drift connect (into,attach) [<name> [<ssh-args> ...]] [flags]

Pick a circuit or kart and connect (merged picker).

Arguments:
  [<name>]            Kart name; omit on a TTY to pick from a merged circuits +
                      karts list.
  [<ssh-args> ...]    Extra flags forwarded to ssh (e.g. -- -i ~/.ssh/id_lab).
                      Under mosh, wrapped into --ssh="ssh …" for the bootstrap.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).
      --no-forwards       Skip the workstation-side ports reconcile for this
                          session only.
      --keep-forwards     Don't tear down forwards on session exit (persist past
                          disconnect).
error: drift connect requires a kart name (non-interactive)
exit status 1
```

### `drift delete --help`

```text
Usage: drift delete [<name>] [flags]

Delete a kart (picker when no name).

Arguments:
  [<name>]    Kart name or shell-style glob (e.g. 'test-*'); omit on a TTY to
              pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -y, --yes               Skip the interactive confirmation prompt.
error: drift delete requires a kart name (non-interactive)
exit status 1
```

### `drift help --help`

```text
Usage: drift help [flags]

Print an LLM-friendly command + protocol reference.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --full              Print the full Kong-derived catalog, including RPC
                          methods.
drift — Devpod for drifters
Remote devcontainers tuned for life on the move — persistent, agentic, phone-friendly

▶  Full catalog:  drift help --full

COMMANDS
  init                            Interactive first-time setup wizard (circuits + characters)
  status                          Circuits + lakitu health + per-circuit karts
  connect [<name>]                Mosh/ssh into a circuit or kart (merged picker)
  new <name>                      Create a kart (from starter or existing repo)
  karts                           List karts across circuits (cross-circuit; -c scopes)
  start | stop | delete [<name>]  Lifecycle shortcuts; bare drops into the cross-circuit kart picker. delete accepts shell globs (e.g. 'test-*')
  kart <verb> <name>              Full lifecycle: start / stop / restart / recreate / rebuild / delete / logs / info / enable / disable
  ports [<verb>]                  Workstation-side TCP port forwards (list/add/rm/remap/up/down/status)
  ai                              Launch Claude Code on the circuit
  skill [<name> [prompt]]         Pick / invoke a Claude skill (`drift skills` to list)
  run [<name>]                    Execute a user-script shorthand (`drift runs` to list)
  circuits                        List configured circuits
  circuit <verb>                  Manage circuits: add / rm / set (name|default) / connect
  migrate                         Adopt an existing devpod workspace as a drift kart
  update                          Check GitHub for a newer drift and self-install

Run `drift <cmd> --help` for per-command flags
```

### `drift init --help`

```text
Usage: drift init [flags]

Interactive first-time setup wizard (circuits + characters).

Flags:
  -h, --help               Show context-sensitive help.
      --[no-]debug         Verbose output (default on; --no-debug to silence)
                           ($DRIFT_DEBUG).
      --no-color           Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING     Target circuit (overrides default).
  -o, --output="text"      Output format for structured commands.
  -v, --version            Print drift version and exit.

      --skip-circuits      Skip the circuit phase (assume already configured).
      --skip-characters    Skip the character phase.
      --no-probe           Skip the server.version probe (offline setup).
error: drift init requires a TTY on stdin (scripted equivalents: drift circuit add; lakitu character new / chest new)
exit status 2
```

### `drift kart connect --help`

```text
Usage: drift kart connect [<name> [<ssh-args> ...]] [flags]

Connect to a kart via mosh (ssh fallback).

Arguments:
  [<name>]            Kart name; omit on a TTY to pick from a cross-circuit kart
                      list.
  [<ssh-args> ...]    Extra flags forwarded to ssh (e.g. -- -i ~/.ssh/id_lab).
                      Under mosh, wrapped into --ssh="ssh …" for the bootstrap.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).
      --no-forwards       Skip the workstation-side ports reconcile for this
                          session only.
      --keep-forwards     Don't tear down forwards on session exit (persist past
                          disconnect).
error: drift kart connect requires a kart name (non-interactive)
exit status 1
```

### `drift kart delete --help`

```text
Usage: drift kart delete [<name>] [flags]

Delete a kart (errors if missing).

Arguments:
  [<name>]    Kart name or shell-style glob (e.g. 'test-*'); omit on a TTY to
              pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -y, --yes               Skip the interactive confirmation prompt.
error: drift delete requires a kart name (non-interactive)
exit status 1
```

### `drift kart disable --help`

```text
Usage: drift kart disable <name>

Disable kart autostart (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift kart enable --help`

```text
Usage: drift kart enable <name>

Enable kart autostart on circuit reboot (idempotent).

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift kart info --help`

```text
Usage: drift kart info <name>

Show a single kart's info.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift kart logs --help`

```text
Usage: drift kart logs <name> [flags]

Fetch a chunk of kart logs.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

  -n, --tail=INT          Show the last N lines (0 = server default).
      --since=DURATION    Only records newer than duration (e.g. 10m, 1h).
                          JSONL only.
      --level=STRING      Minimum level (debug|info|warn|error). JSONL only.
      --grep=STRING       Substring match on msg (JSONL) or raw line (text).
drift: expected "<name>"
exit status 2
```

### `drift kart rebuild --help`

```text
Usage: drift kart rebuild <name>

Re-apply the current tune to a kart and recreate its container.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift kart recreate --help`

```text
Usage: drift kart recreate <name>

Recreate a kart's container (no tune re-apply) — picks up devcontainer.json
changes.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift kart restart --help`

```text
Usage: drift kart restart <name>

Restart a kart.

Arguments:
  <name>    Kart name.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
drift: expected "<name>"
exit status 2
```

### `drift karts --help`

```text
Usage: drift karts [flags]

List karts (cross-circuit by default; scope with -c).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
→ /home/circuit/.drift/bin/devpod list --output json
CIRCUIT  NAME         STATUS         SOURCE
alpha    test-nixenv  running        clone https://github.com/kurisu-agent/drift
alpha    test2        error (stale)  clone https://github.com/kurisu-agent/tzone-buddy
```

### `drift kart start --help`

```text
Usage: drift kart start [<name>]

Start a kart (idempotent).

Arguments:
  [<name>]    Kart name; omit on a TTY to pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift start requires a kart name (non-interactive)
exit status 1
```

### `drift kart stop --help`

```text
Usage: drift kart stop [<name>]

Stop a kart (idempotent).

Arguments:
  [<name>]    Kart name; omit on a TTY to pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift stop requires a kart name (non-interactive)
exit status 1
```

### `drift migrate --help`

```text
Usage: drift migrate [flags]

Adopt an existing devpod workspace as a drift kart (interactive).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift migrate requires an interactive terminal
exit status 1
```

### `drift new --help`

```text
Usage: drift new <name> [flags]

Create a new kart (from starter or existing repo).

Arguments:
  <name>    Kart name (matches ^[a-z][a-z0-9-]{0,62}$).

Flags:
  -h, --help                   Show context-sensitive help.
      --[no-]debug             Verbose output (default on; --no-debug to
                               silence) ($DRIFT_DEBUG).
      --no-color               Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING         Target circuit (overrides default).
  -o, --output="text"          Output format for structured commands.
  -v, --version                Print drift version and exit.

      --clone=STRING           Clone an existing repo (mutually exclusive with
                               --starter).
      --starter=STRING         Template repo; history is discarded after clone.
      --tune=STRING            Named preset that provides defaults for other
                               flags.
      --features=STRING        Devcontainer features JSON, merged with the
                               tune's (additive).
      --devcontainer=STRING    Override devcontainer: file path, JSON string,
                               or URL.
      --dotfiles=STRING        Layer-2 dotfiles repo URL (overrides tune's
                               dotfiles_repo).
      --character=STRING       Git/GitHub identity to inject.
      --autostart              Enable auto-start on server reboot.
      --mount=MOUNT            Extra host-bind or volume mount (repeatable).
                               type=bind,source=X,target=Y
      --[no-]connect           Connect to the kart after a successful create
                               (disable with --no-connect).
drift: expected "<name>"
exit status 2
```

### `drift ports add --help`

```text
Usage: drift ports add <port> [flags]

Add a forward (remaps on local-port conflict).

Arguments:
  <port>    In-kart port to forward.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --local=INT         Workstation port (defaults to PORT; auto-remaps on
                          conflict).
      --kart=STRING       "<circuit>/<kart>" or just "<kart>" with -c.
drift: expected "<port>"
exit status 2
```

### `drift ports down --help`

```text
Usage: drift ports down [flags]

Cancel forwards and stop the master (per-kart, or all when --all).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       "<circuit>/<kart>" — required unless --all.
      --all               Tear down every forward in the state file.
error: drift ports down: --kart or --all is required
exit status 1
```

### `drift ports list --help`

```text
Usage: drift ports list [flags]

Show forwards (table or JSON; bare "drift ports" runs this).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       Restrict to one kart ("<circuit>/<kart>" or just
                          "<kart>" with -c).
no forwards configured
```

### `drift ports probe --help`

```text
Usage: drift ports probe [flags]

Probe in-kart listeners and pick which to forward.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       "<circuit>/<kart>" or just "<kart>" with -c.
      --all               Skip the picker; add every detected port.
error: drift ports probe requires a kart name (non-interactive)
exit status 1
```

### `drift ports remap --help`

```text
Usage: drift ports remap <spec> [flags]

Change the local port for an existing forward (REMOTE:LOCAL).

Arguments:
  <spec>    REMOTE:LOCAL — change the workstation port for an existing forward.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       "<circuit>/<kart>" or just "<kart>" with -c.
drift: expected "<spec>"
exit status 2
```

### `drift ports rm --help`

```text
Usage: drift ports rm (remove,delete) <port> [flags]

Remove a forward.

Arguments:
  <port>    In-kart port (or local port if not found as remote).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       "<circuit>/<kart>" or just "<kart>" with -c.
drift: expected "<port>"
exit status 2
```

### `drift ports status --help`

```text
Usage: drift ports status

Print state + per-kart master liveness as JSON.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
{
  "karts": [],
  "version": 1
}
```

### `drift ports up --help`

```text
Usage: drift ports up [flags]

Reconcile state to live forwards (no-op when already in sync).

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --kart=STRING       Limit reconcile to one kart.
```

### `drift run --help`

```text
Usage: drift run [<name> [<args> ...]] [flags]

Execute a user-script shorthand from runs.yaml.

Arguments:
  [<name>]        Shorthand name (see drift runs); omit to pick interactively.
  [<args> ...]    Positional args forwarded to the remote command.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh) for interactive entries.
      --forward-agent     Enable SSH agent forwarding (-A).
no runs configured on this circuit
  edit ~/.drift/runs.yaml on the circuit to add entries
```

### `drift runs --help`

```text
Usage: drift runs [flags]

List runs.yaml entries on the target circuit.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
no runs configured on this circuit
  edit ~/.drift/runs.yaml on the circuit to add entries
```

### `drift skill --help`

```text
Usage: drift skill [<name> [<prompt> ...]] [flags]

Pick / invoke a Claude skill on the circuit.

Arguments:
  [<name>]          Skill name (see drift skill); omit to list.
  [<prompt> ...]    Initial prompt forwarded to the skill.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --ssh               Force plain SSH (skip mosh).
      --forward-agent     Enable SSH agent forwarding (-A).
no skills on this circuit
  drop SKILL.md files into ~/.claude/skills/<name>/ on the circuit
```

### `drift skills --help`

```text
Usage: drift skills [flags]

List Claude skills on the target circuit.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
no skills on this circuit
  drop SKILL.md files into ~/.claude/skills/<name>/ on the circuit
```

### `drift start --help`

```text
Usage: drift start [<name>] [flags]

Start a kart (picker when no name).

Arguments:
  [<name>]    Kart name; omit on a TTY to pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift start requires a kart name (non-interactive)
exit status 1
```

### `drift status --help`

```text
Usage: drift status [flags]

Show configured circuits + their lakitu health and per-circuit karts.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --no-probe          Skip the server.status round-trip (show client state
                          only).
→ /home/circuit/.drift/bin/devpod list --output json
drift devel
default: alpha

   CIRCUIT  HOST                LAKITU  API  LATENCY  KARTS
→  alpha    circuit@10.233.1.2  dev     1    733ms    1/2

→ alpha
NAME         STATUS         SOURCE                                             TUNE    AUTOSTART
test-nixenv  running        clone https://github.com/kurisu-agent/drift        nixenv  
test2        error (stale)  clone https://github.com/kurisu-agent/tzone-buddy  -       
```

### `drift stop --help`

```text
Usage: drift stop [<name>] [flags]

Stop a kart (picker when no name).

Arguments:
  [<name>]    Kart name; omit on a TTY to pick from a cross-circuit kart list.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.
error: drift stop requires a kart name (non-interactive)
exit status 1
```

### `drift update --help`

```text
Usage: drift update [<source>] [flags]

Check GitHub for a newer drift release and self-install.

Arguments:
  [<source>]    Dev override: pull the drift binary from an scp path, URL,
                or local file instead of fetching the latest GitHub release.

Flags:
  -h, --help              Show context-sensitive help.
      --[no-]debug        Verbose output (default on; --no-debug to silence)
                          ($DRIFT_DEBUG).
      --no-color          Disable ANSI colors in text output ($NO_COLOR).
  -c, --circuit=STRING    Target circuit (overrides default).
  -o, --output="text"     Output format for structured commands.
  -v, --version           Print drift version and exit.

      --check             Check for an update without downloading.
→ checking latest release
current: devel
latest:  0.10.0
error: refusing to self-update a development build; rebuild from source or install a tagged release
exit status 1
```

